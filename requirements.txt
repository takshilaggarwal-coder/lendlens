# RAG branch (rag-anthropic) — live grounded Q&A via Claude
streamlit>=1.36
numpy>=1.26
pandas>=2.0
plotly>=5.20
python-dotenv>=1.0
anthropic>=0.40        # required on this branch: Claude synthesizes Q&A answers

# Optional — dense embeddings join the retrieval fusion when present
# openai>=1.40
